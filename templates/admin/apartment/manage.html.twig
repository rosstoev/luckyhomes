{% extends 'admin/base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="col-12">
            {{ form_start(form) }}
            <div class="form-group row">
                <div class="col-lg-6 col-sm-12">
                    {{ form_label(form.name, 'Апартамент') }}
                    {{ form_row(form.name, {'attr' : {'class': 'form-control'}}) }}
                </div>
            </div>
            <div class="form-group row">
                <div class="col-lg-6 col-sm-12">
                    {{ form_label(form.buildingArea, 'Застроена площ') }}
                    {{ form_row(form.buildingArea, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.totalParts, 'Общи части') }}
                    {{ form_row(form.totalParts, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.totalArea, 'Обща площ') }}
                    {{ form_row(form.totalArea, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.priceUnfinish, 'Цена незавършен') }}
                    {{ form_row(form.priceUnfinish, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.priceFinish, 'Цена завършен') }}
                    {{ form_row(form.priceFinish, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.priceRent, 'Цена наем') }}
                    {{ form_row(form.priceRent, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.status, 'Статус') }}
                    {{ form_row(form.status, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.floor, 'Етаж') }}
                    {{ form_row(form.floor, {'attr' : {'class': 'form-control'}}) }}

                    {{ form_label(form.additionalInfo, 'Допълнителна информация') }}
                    {{ form_row(form.additionalInfo, {'attr' : {'class': 'form-control'}}) }}
                </div>
            </div>
            <div class="form-group row">
                <div class="col-lg-4">
                    {{ form_label(form.images, 'Качи снимки') }}
                    {{ form_row(form.images, {'attr' : {'class': 'form-control'}}) }}
                </div>
            </div>
            {#<div class="form-group row">#}
                {#<div class="col-12">#}
                    {#<label for="img-selector"><?=$imgLabel?></label>#}
                    {#<select id="img-selector" multiple="multiple" name="img-select[]" class="image-picker">#}
                        {#<?php for($x=2;$x<count($allImg);$x++) :?>#}
                        {#<option data-img-src="<?='../'.$pathImg.$apartment_id.'/'.$allImg[$x]?>"#}
                                {#value="<?=$allImg[$x]?>"><?=$allImg[$x]?></option>#}
                        {#<?php endfor; ?>#}
                    {#</select>#}
                {#</div>#}
            {#</div>#}
            <div class="form-group row">
                <button id="edit-submit" type="submit" name="submit" class="btn btn-dark">Запази</button>
                <span id="loader"></span>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}